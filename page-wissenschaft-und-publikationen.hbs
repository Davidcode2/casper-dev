{{!< default}}

{{!-- The tag above means: insert everything in this file
into the {body} tag of the default.hbs template --}}


{{#post}}
{{!-- Everything inside the #post block pulls data from the page --}}

<main id="site-main" class="site-main">
<article class="article {{post_class}}">

    {{#match @page.show_title_and_feature_image}}
        <header class="!mb-20">

            {{#if feature_image}}
                <figure class="relative">
                    {{!-- This is a responsive image, it loads different sizes depending on device
                    https://medium.freecodecamp.org/a-guide-to-responsive-images-with-ready-to-use-templates-c400bd65c433 --}}
                    <img
                        class="w-full"
                        srcset="{{img_url feature_image size="s"}} 300w,
                                {{img_url feature_image size="m"}} 600w,
                                {{img_url feature_image size="l"}} 1000w,
                                {{img_url feature_image size="xl"}} 2000w"
                        sizes="(min-width: 1400px) 1400px, 92vw"
                        src="{{img_url feature_image size="xl"}}"
                        alt="{{#if feature_image_alt}}{{feature_image_alt}}{{else}}{{title}}{{/if}}"
                    />
                    {{#if feature_image_caption}}
                        <figcaption>{{feature_image_caption}}</figcaption>
                    {{/if}}
                    <h1 class="article-title !p-10 absolute bottom-0 rounded bg-radial from-white/40 via-white/20 to-transparent">{{title}}</h1>
                </figure>
            {{/if}}
        </header>
    {{/match}}

    <section class="gh-content gh-canvas">
            {{^if feature_image}}
              <h1 class="article-title">{{title}}</h1>
            {{/if}}
        {{content}}
    </section>

</article>

<div id="site-main" class="site-main outer">
  <div class="inner posts">


      <div class="post-feed !mb-20 lg:!mb-42">
        {{!-- Get posts excluding certain tags --}}
        {{!-- {{#get "posts" filter="tag:fiction+tag:-fables"}} --}}

        {{#get "posts" filter="tag:publikation" include="tags,authors" }}
                {{#foreach posts}}
                    {{!-- The tag below includes the markup for each post - partials/post-card.hbs --}}
                    {{> "post-card"}}
                {{/foreach}}
            {{pagination}}
        {{/get}}
      </div>
  </div>
</div>

</main>

{{/post}}
